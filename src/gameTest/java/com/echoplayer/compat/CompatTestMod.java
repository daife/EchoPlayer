package com.echoplayer.gametest;

import net.minecraftforge.fml.common.Mod;

/** Test-only mod container so Forge scans the isolated GameTest source set. */
@Mod("echoplayer_gametest")
public final class CompatTestMod {}
